{
  "buildCommand": "pnpm install && pnpm --filter database exec prisma generate && pnpm --filter database exec prisma db seed && pnpm run build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}