{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "buildCommand": "cd ../.. && pnpm install && pnpm db:generate && cd apps/customer && pnpm build",
  "outputDirectory": ".next",
  "env": {
    "SKIP_BUILD_STATIC_GENERATION": "true"
  }
}
