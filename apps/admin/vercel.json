{
  "buildCommand": "pnpm --filter database exec prisma generate && pnpm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD apps/admin packages"
}